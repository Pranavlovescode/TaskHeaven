# Neon PostgreSQL
spring.datasource.url=jdbc:postgresql://ep-little-frost-adop5ceh.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_d79yafeYxPlr
spring.datasource.driver-class-name=org.postgresql.Driver

# HikariCP settings
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=600000
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.validation-timeout=1000
spring.datasource.hikari.connection-test-query=SELECT 1
spring.datasource.hikari.leak-detection-threshold=30000

# Hibernate settings
spring.jpa.properties.hibernate.connection.release_mode=after_transaction
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false
spring.jpa.properties.hibernate.hbm2ddl.auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Mailtrap SMTP
spring.mail.host=bulk.smtp.mailtrap.io
spring.mail.port=587
spring.mail.username=api
spring.mail.password=022e0b031c033aaac088ed855d018167
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Session settings
server.servlet.session.timeout=30m
server.servlet.session.cookie.same-site=NONE
server.servlet.session.cookie.secure=true
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.domain=pranavtitambe.in

# Cookie configuration
app.cookie.domain=pranavtitambe.in
app.cookie.secure=true

# Frontend URL for CORS
app.frontend.url=https://taskheaven.pranavtitambe.in
spring.web.cors.allowed-origins=https://taskheaven.pranavtitambe.in
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true
spring.web.cors.max-age=3600
